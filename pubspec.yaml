name: ui_alumni_mobile
description: "IU Alumni mobile app — WebView shell for the Nuxt web application."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.8.1
  flutter: ^3.32.2

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true


